{
  "devDependencies": {
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "@types/express": "^4.17.14",
    "express": "^4.18.2",
    "quizlet-fetch": "^1.0.4",
    "vanilla-tsx": "^0.1.3"
  },
  "scripts": {
    "dev": "cd packages/web && nodemon server/src/index.ts",
    "build": "cd packages/web/server && npx tsc && cd ../public && npx tsc && cd ../../quizlet-fetch && npx tsc"
  }
}
